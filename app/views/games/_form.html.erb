<%= form_with(data: { insert_target: "form", action: "submit->insert#send" }) do |form| %>
<%# <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<%= hidden_field_tag 'token', @animal.name %>
  <%= form.text_field :answer %>
  <%= form.submit "submit" %> %>
<% end %>
