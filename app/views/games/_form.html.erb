<%= form_with(data: { action: "submit->result#show", remote: true }) do |form| %>
<%= hidden_field_tag 'token', @animal.name %>
  <%= form.text_field :answer, placeholder: "Your guess goes here..." %>
  <%= form.submit "GO", class: 'btn-submit' %>
<% end %>
